import 'package:flutter/material.dart';

class SellerVerifyPaymentsScreen extends StatelessWidget {
  const SellerVerifyPaymentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Verify Payments')),
      body: const Center(
        child: Text('Seller payment verification screen placeholder.'),
      ),
    );
  }
}
